# Example environment file for Inception project
# Copy this file to .env and configure your values

DOMAIN_NAME=yourdomain.42.fr

# DATABASE SETUP
SQL_DATABASE=wordpress
SQL_USER=wpuser

# WordPress configuration
WORDPRESS_DB_HOST=mariadb:3306
WORDPRESS_DB_NAME=wordpress

# Note: Passwords are managed via Docker secrets in ../secrets/ directory
# You need to create:
# - ../secrets/db_password.txt (MySQL user password)
# - ../secrets/db_root_password.txt (MySQL root password)  
# - ../secrets/credentials.txt (WordPress users - format: user:password per line)